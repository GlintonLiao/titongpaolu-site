import React, {useContext} from 'react'

const gallery = () => {
  // const data = useContext();
  return (
    <div>
      {imgs.map((img, _idx) => (
        <div key={_idx}>
          <p>aklsdhkla</p>
        </div>
      ))}
    </div>
  )
}

export default gallery
