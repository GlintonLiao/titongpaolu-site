import React, {useContext} from 'react'

const gallery = () => {
  // const data = useContext();
  return (
    <div>
      {imgs.map(img, _idx) => (
        <Image />
      )}
    </div>
  )
}

export default gallery